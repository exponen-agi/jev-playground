# Required for every cookbook.
# Early access: https://typesafe.ai
TYPESAFE_API_KEY=sk-ts-...

# Pin the version once your thresholds are tuned. Aliases move.
TYPESAFE_MODEL=jev-1.13.0

# Optional. Each cookbook runs its Jev half without these and tells you
# which half it skipped.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=
